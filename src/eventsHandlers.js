const storyDeleteHandler = require('./eventsHandlers/storyDeleteHandler');
const storyInsertHandler = require('./eventsHandlers/storyInsertHandler');
const storyUpdateHandler = require('./eventsHandlers/storyUpdateHandler');

module.exports = {
  storyDeleteHandler,
  storyInsertHandler,
  storyUpdateHandler,
}